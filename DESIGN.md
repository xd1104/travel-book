# 旅途手帳（travel-planner）設計規格 v3

> lab-ux 產出，供 lab-dev 正式開發依據。demo 在 `demo/index.html`（單檔、零依賴、雙擊即玩）。
> **狀態：v2 已過 Benson，v3 加入三項新需求（營業時間選擇器／打包模板／首頁分區）；v3.1 依 Benson 回饋重設「已結束」區視覺（旅行回憶歸檔卡），待試玩確認。**

## 1. 產品定位

手機優先的旅遊行程規劃 app。一個旅程 = 逐日行程 + 花費 + 打包清單 + 自由備註。
架構預期沿用 recipe-book 三層：本機 Node App 為真本 + GitHub repo 同步 + GitHub Pages PWA 給手機。

## 2. 畫面清單與導覽結構

```
首頁
 ├─ 主區：進行中＋未出發的旅程卡（依出發日近→遠排序）
 ├─ ＋ 規劃新旅程（sheet；含「打包模板」選擇，預設不使用）
 ├─ 「已結束的旅程（N）」收合區：預設收起，展開後卡片灰階淡化（依日期自動判定，非手動標記）
 └─ 點卡片 → 旅程頁
旅程頁（漸層 header + 底部 4 tab）
 ├─ header 右上「✎ 編輯」→ 編輯旅程 sheet（帶入現值；無模板欄）
 ├─ 行程 tab：Day chips（sticky）→ timeline
 │   ├─ 行程點卡：時間 + 類別 + 名稱 + 地點 + 備註 + 🗺️ 地圖鈕
 │   ├─ 點卡片 → 詳細 sheet（只列有值欄位）→ ✎ 編輯（完整表單）
 │   ├─ 「調整」模式：✕ 刪除 + ☰ 拖曳把手
 │   └─ FAB ＋ → 新增行程點（精簡表單）
 ├─ 花費 tab：預算卡 → 分類小計 → 紀錄；FAB ＋ 記一筆
 ├─ 打包 tab：分區 segmented（🧳 行李托運 / 🎒 隨身）→ 快速新增列
 │   ├─ 「📦 從模板帶入」sheet：模板列表＋帶入（自動跳過同名項目）
 │   ├─ 「管理模板」sheet：列表＋編輯/刪除＋新增模板
 │   │    └─ 模板編輯 sheet：改名、分區新增項目、刪項目、儲存
 │   ├─ 清單全空時額外給大顆「從模板帶入一套」CTA
 │   └─ 兩分區各自標題＋已打包 x/y＋checklist
 └─ 備註 tab：整頁 textarea 自動儲存
```

## 3. 關鍵互動規格

### 3.1 行程點詳細頁（sheet）
- 檢視欄位（空值不顯示）：地點、預估費用、訂位/票券代號、電話（tel:）、官網/參考、營業時間、詳細備註。
- 編輯完整欄位：名稱*、時間、類別、地點、Google Maps 連結、預估費用、訂位/票券代號、聯絡電話、**營業時間（開/關兩個 time picker＋「24 小時營業」勾選）**、官網/參考連結、詳細備註。
- URL 欄 `type=text`+`inputmode=url`，讀取時自動補 https://。

### 3.2 營業時間（v3 改為選擇式）
- 結構化欄位：`hoursOpen`／`hoursClose`（原生 `<input type="time">`，iOS 跳系統轉輪）＋ `hours24`（checkbox，勾了即 disable 兩個 time 欄）。
- 顯示規則：`hours24` → 「24 小時營業」；有 open/close → 「10:00–22:30」（缺一邊補「？」）；都沒有 → 顯示舊自由文字 `hours`（**向下相容**）。
- 儲存規則：只要填了任何結構化值就清空舊 `hours`；表單開啟時若只有舊文字，顯示提示「原本記的文字：…選了時間就會取代它」。

### 3.3 Google Maps 跳轉
mapUrl 優先 → place 文字搜尋 fallback → 皆無不顯示；`target=_blank rel=noopener`；卡上按鈕 stopPropagation。

### 3.4 拖曳排序（調整模式）
純 pointer events、只把手 ☰ 可拖（`touch-action:none`）、被拖卡浮起＋其他卡平移讓位、pointercancel 復原。長清單邊緣自動捲動留正式版。

### 3.5 編輯旅程
共用新增表單（hidden editId）；縮短天數不刪資料（day-key 保留＋固定提示）；`ui.day` 超界 clamp。編輯模式**不顯示**打包模板欄（避免誤蓋既有清單，既有旅程套模板走打包 tab）。

### 3.6 打包分區與模板
- zone：`checked`（🧳 行李/托運)、`carry`（🎒 隨身）；segmented 決定新增目標區，記住本場選擇。
- **帶入模板**：把模板項目 append 進旅程打包清單，**同名（trim 後相等）項目自動跳過**，不動已勾狀態。
- **模板管理**：改名、分區增刪項目、新增自訂模板、刪除模板（刪除不影響已帶入的項目，會 confirm）。
- **新增旅程時**：表單「打包模板」select（預設「不使用」），建立時自動帶入。

### 3.7 首頁分區（自動、依日期）——v3.1 視覺重設
- 判定：`結束日（start + days - 1）< 今天` → 已結束；其餘（未出發＋進行中）在主區。
- 主區排序：出發日近的在前；已結束區：最近的在前。
- **已結束區＝「📔 旅行回憶」歸檔卡**（設計決策：已結束是回憶不是壞掉的現役卡，捨棄灰階大卡）：
  - 一張白卡容器：標題列（📔 旅行回憶＋數量 badge＋旋轉 chevron，min-height 56px）＋收合列表。
  - 每趟＝**精簡列表列**（min-height 60px）：42px 圓角 emoji 磚（沿用該旅程漸層色、`saturate(.85)` 微降彩度）＋名稱＋`YYYY/M/D・N 天`＋右側總花費＋`›`。跟主區大卡拉開層級，但保留每趟的顏色記憶。
  - 展開/收起動畫：列永遠在 DOM，外層 `grid-template-rows 0fr↔1fr` 過渡（0.28s）；toggle 就地切 class 不整頁重繪，chevron 同步旋轉；下次全頁 render 時 markup 依 `ui.showEnded` 重建、狀態一致。
  - 點列仍進完整旅程頁，內容不變。
- 空狀態：主區空但有已結束 → 「目前沒有進行中或即將出發的旅程」；全空 → 「還沒有任何旅程」。

## 4. 視覺語言（已定調）

暖米白底 `#f7f4ee`、珊瑚紅主色 `#ff6b5e`、圓角 14–20、軟陰影；每旅程一組漸層主題＋emoji；類別固定色；空狀態＝大 emoji＋一句人話＋CTA。

## 5. 資料模型草案（比照 recipe-book：md + frontmatter）

```
data/
  templates/<slug>.md          # 打包模板：frontmatter: name
                               # frontmatter list: items: [{text, zone}]  zone: carry|checked
  trips/<slug>/
    trip.md                    # frontmatter: name, dest, emoji, theme, start, days, budget
                               # body: 自由備註
    stops/<day>-<seq>-<slug>.md
        # frontmatter: day, order, time, title, cat, place,
        #              mapUrl, cost, bookingRef, phone, url,
        #              hoursOpen, hoursClose, hours24, hours(舊文字相容)
        # body: 詳細備註
    expenses.md                # frontmatter list: [{amount, cat, desc, date}]
    packing.md                 # frontmatter list: [{text, done, zone}]
```

- 模板是**全域資源**（跨旅程），一個模板一個 md 檔；「帶入」＝複製項目進該旅程 packing（複製後與模板脫鉤，改模板不回溯）。
- demo 對應：同構 JSON 存 localStorage `travel-demo-v3`；載入時自動遷移 `v2`/`v1`（packing 補 zone → carry；缺 templates → 補三個內建模板），不噴錯。
- 內建模板三款（可被使用者改/刪）：出國基本款（11 項）、國內小旅行（7 項）、海邊／溫泉（8 項），項目皆已分區。

## 6. 鐵律（recipe-book 踩過的雷，正式版必守）

1. 所有 input/textarea/select `font-size ≥ 16px`。
2. 所有資源相對路徑（Pages 子路徑）。
3. 觸控目標 ≥ 44px；底部固定列 safe-area padding。
4. Enter 送出用原生 `<form>` + `type=submit`。
5. PWA：SW skipWaiting＋清舊快取＋clients.claim；資料 network-first。
6. 手機端讀寫走帶 token 認證 API；寫入後樂觀更新。
7. 拖曳把手 `touch-action:none`、只把手可拖。
8. 資料 schema 演進一律「新 key＋讀舊 key 遷移」，遷移函式冪等。

## 7. demo 省略、正式版要做

- 三層架構：本機 Node App、git 同步、Pages PWA + PAT 設定頁。
- 內嵌地圖（現為 deep link）。
- 拖曳長清單邊緣自動捲動。
- 旅程刪除/封存；刪除防呆（undo/confirm）。
- 花費：日期、多幣別、按天檢視；行程點預估費用 vs 實際花費對照（加分項）。
- 模板進階：套用時預覽勾選要哪幾項、模板排序。
- AI 功能（接本機 claude）：生行程草稿、貼訂房信抽欄位。
- 資料匯出。

## 8. 拍板狀態

- A. 行程排序哲學 → **已定案：手動（拖曳）**。
- B. 花費綁天/行程點：仍開放。
- C. AI 功能優先級：仍開放（UX 推薦排 v2 之後）。
- 首頁已結束分區 → **已定案：依日期自動判定**（PM 已與 Benson 確認）。

---

# 附錄 A（v2.6 提案）— 地圖／路線圖示 ＋ 手機端地圖連結欄

> lab-ux 產出，供 lab-dev 照做。可試玩 demo：**`demo/icons-and-maplink.html`**（單檔、零依賴、雙擊開；`demo/index.html` 不動）。
> 狀態：**待 Benson 試玩拍板**。拍板前 dev 先不要動 `public/`。

## A1. 題目一：把 🗺️ emoji 換成 inline SVG

### A1.1 為什麼

`.map-btn` 目前是一顆 🗺️ emoji。這個 App 的視覺語言是暖米白＋珊瑚紅＋圓角＋軟陰影，emoji 是彩色點陣、風格不受控、**每支手機長得還不一樣**（iOS／Android／Windows 三套），而且同一顆圖示同時被用在「開這個地點的地圖」與「看這一段路線」兩個**不同的動作**上。

改用 **inline SVG**：吃 `currentColor`（顏色由 CSS 決定、跟色票一致）、每台裝置長得一樣、線條粗細可以跟 UI 的圓角軟調對齊、兩個動作可以用不同輪廓分開。

### A1.2 圖示界線（**這條寫給下一個接手的人，別把整個 App 的 emoji 都拔掉**）

| 類型 | 例子 | 處置 |
|---|---|---|
| **功能鈕上的 emoji**（可點、會觸發動作、有 `aria-label`） | `.map-btn` 的 🗺️、詳細 sheet `.btn-ghost` 的「🗺️ 開啟 Google 地圖」 | **換成 inline SVG** |
| **內容型 emoji**（是資料、是使用者自己選的、有語意） | 類別 emoji（`CATS[].emoji` 🍜📸🚗…）、旅程封面 emoji、tab bar、`.ao-ico`、詳細列前的 📍⏱️💰📞🔗🕘📝、`.empty .big` | **一律保留，不准動** |
| **單色符號字元**（不是彩色 emoji） | `.tool-btn` 的 ✕、`.drag-handle` 的 ☰、`✎`、FAB 的 ＋ | **保留**（本來就單色、跨裝置一致，換 SVG 沒有收益） |

一句話規則：**「使用者選的＝內容，留著；系統給的功能鈕＝介面，用 SVG。」**

### A1.3 兩顆圖示（最終 path，直接抄）

共同屬性：`viewBox="0 0 24 24"`、`fill:none`、`stroke:currentColor`、`stroke-linecap/linejoin:round`、`aria-hidden="true" focusable="false"`。

**① 地點 pin —「這個地方在哪」**（行程點卡片右上、詳細 sheet 的 `.btn-ghost`）

```html
<svg viewBox="0 0 24 24" class="ico" aria-hidden="true" focusable="false">
  <path d="M12 21.2c4.2-4.5 6.3-8 6.3-10.6a6.3 6.3 0 1 0-12.6 0c0 2.6 2.1 6.1 6.3 10.6Z"/>
  <circle cx="12" cy="10.4" r="2.4"/>
</svg>
```

**② 路線 —「從這裡到那裡怎麼走」**（移動灰條右邊）＝起點空心圓 ＋ 圓角轉彎 ＋ 箭頭

```html
<svg viewBox="0 0 24 24" class="ico" aria-hidden="true" focusable="false">
  <circle cx="6.2" cy="18.4" r="2.6"/>
  <path d="M6.2 15.8V11.4A3.4 3.4 0 0 1 9.6 8h7.3"/>
  <path d="M14.4 5.5 17.3 8l-2.9 2.5"/>
</svg>
```

輪廓刻意差很多（一顆水滴 vs 一條帶箭頭的折線），縮到 19px 也分得出來。

`aria-label` 一起改精準：
- 卡片那顆：`aria-label="在地圖上看這個地點"`（原本是「開啟地圖」）
- 灰條那顆：`aria-label="看這一段路線"`

### A1.4 CSS（加在 `styles.css` 的 `.map-btn` 那一段旁邊）

```css
/* v2.6：功能鈕圖示改 inline SVG（吃 currentColor，跨裝置一致） */
.map-btn .ico{width:22px; height:22px; display:block; fill:none; stroke:currentColor;
  stroke-width:1.8; stroke-linecap:round; stroke-linejoin:round;}
.transit-bar .map-btn .ico{width:19px; height:19px; stroke-width:1.9;}
.btn-ghost .ico{width:19px; height:19px; fill:none; stroke:currentColor;
  stroke-width:1.8; stroke-linecap:round; stroke-linejoin:round;}
```

`.map-btn` 本體的 `width/height/margin/border-radius` **一行都不改**（44px、灰條上 38px ＋ `::before inset:-3px` 外擴回 44px 的既有決策照舊）。`font-size:20px`／`17px` 已經沒有作用，可留可刪。

**顏色＝三個候選，等 Benson 拍板選一個**（demo 上可即時切）：

| | 卡片上的地點鈕 | 灰條上的路線鈕 | 個性 |
|---|---|---|---|
| **A 純線條** | `background:none; color:#a1968a`（`:active` `#f3eee4`） | `background:none; color:#9a9082` | 最安靜，不搶卡片 |
| **B 淡珊瑚底**（UX 推薦） | `background:#fff1ef; color:var(--acc-deep)`（`:active` `#ffe3df`） | `background:#fbeae7; color:var(--acc-deep)`（`:active` `#f6ddd8`） | 跟 `.count-chip`／`.cat-pill` 同一套 tinted pill 語言，最像「這裡可以點」 |
| **C 中性淺底** | `background:#f3eee4; color:#6b6154`（`:active` `#e9e1d2`） | `background:#e4ddcf; color:#6f6558`（`:active` `#d8cfbd`） | 跟 `.tool-btn`／`.drag-handle` 同家族 |

正式版只留選中那一組，**不要把 demo 的 `data-sty` 切換死碼帶進去**。

### A1.5 灰條左邊那顆 🚶（**選配，要 Benson 拍板**）

`.tr-ico` 現在寫死 `🚶`。換掉右邊的鈕之後，灰條上會變成「彩色 emoji ＋ 單色 SVG」並排，有點不搭。提案：`.tr-ico` 也改成**依 `travelMode(sp.note)` 自動選的線條圖示**（walking→人形／transit→列車／driving→車／bicycling→單車），沿用同一組 stroke 參數（15px、stroke-width 1.8、`color:#9c9284`、`opacity:1`）。demo 有開關可以直接比。

**這是加分項，不做也不影響題目一。** 不做就維持 🚶（它算內容型，不違反 A1.2）。

### A1.6 dev 檢查點

- [ ] `public/app.js` 三處 `🗺️` 全換（`grep -n "🗺️" public/app.js` 要回 0 行）
- [ ] 卡片鈕仍是 44×44、灰條鈕視覺 38px、命中區 `::before inset:-3px` 仍在 → **灰條總高度必須還是 38px**
- [ ] 調整模式（`ui.edit`）一樣不顯示這兩顆鈕
- [ ] 灰條那顆的 `onclick="event.stopPropagation()"` 保留（不能順手打開編輯移動表單）
- [ ] `sw.js` cache 版本號 ＋1、`APP_VER` 同步（PWA 鐵律）

---

## A2. 題目二：手機上換 Google 地圖連結

### A2.1 為什麼

現況是一顆普通 `<input>`，裡面躺著 `https://maps.app.goo.gl/9Yh2Kq7Tf1vRz8xA?g_st=ic`。Benson 的實際流程是**「Google Maps App 分享 → 複製連結 → 回 App 貼上」**，但他得先在手機上把那串長網址一個字一個字刪掉才貼得進去。而且那串網址**人根本看不懂**，佔了一整欄卻沒有資訊量。

v2.4 之後，server／CI 會把這條短連結展開成**地址**存進 `addr`。所以更根本的解法不是「讓網址好刪」，而是**畫面上根本不要顯示網址，顯示地址**——網址退成技術細節。

### A2.2 三個方案（demo 可即時切，等拍板）

| | 做法 | 換連結要幾個動作 | 成本 |
|---|---|---|---|
| **A 已連結卡 ＋ 一鍵貼上**（UX 推薦） | 欄位改成「顯示地址的卡片」＋「貼上新連結／✕ 清除」；貼上直接讀剪貼簿覆蓋 | **1 下**（剪貼簿已有連結時） | 中 |
| **B 欄內 ✕ 清空** | 維持 input，右側加 44px ✕；聚焦自動全選 | 2 下（✕ → 長按貼上） | 小 |
| **C 只做聚焦全選** | 只加 `onfocus` 全選，貼上直接覆蓋 | 2 下（點欄位 → 長按貼上），而且還是看得到醜網址 | 極小 |

**推薦 A**：同時解掉「難刪」與「看不懂」，而且把 v2.4 已經算出來的 `addr` 拿出來用（現在 `addr` 只有路線鈕在吃，使用者從來沒看過它）。B/C 的行為（全選、一鍵清）在 A 的「手動編輯」子狀態裡也保留著，不是丟掉。

### A2.3 方案 A 規格

欄位標題從「Google Maps 連結」改成 **「Google 地圖」**（顯示的是地址不是連結了）。三個狀態：

**狀態 1｜未連結（`!mapUrl`）**

```
Google 地圖
┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
   [貼上圖示] 貼上 Google 地圖連結      ← .m-empty，虛線框，min-height 52
└ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘
沒有連結也沒關係，會用上面的「地點」文字去搜尋。   ← .hint
```

**狀態 2｜已連結且有 `addr`**

```
Google 地圖
┌────────────────────────────────┐
│ [pin] 504彰化縣秀水鄉義興街1號 秀水湯包 │  ← .maddr 15px/700，可換行
│       maps.app.goo.gl 連結           │  ← .msub 12px muted（只顯示 host）
└────────────────────────────────┘
[ 貼上新連結 ]            [ ✕ ]         ← .m-paste（flex:1, 46px）/ .m-clear（46×46）
手動編輯連結                             ← .m-manual，12.5px 底線小字，命中 44px
```

**狀態 3｜已連結但 `addr` 還沒補上**（剛貼上、或 server／CI 展不開的路線型連結）

- `.maddr` 顯示「**地址整理中…**」（`#8a8070`、weight 600）
- `.msub` 顯示「**存檔後由伺服器把連結換成地址**」
- 容器加 class `.pending`
- **文案刻意中性、不寫「失敗」**：Benson 幾乎只用手機，展開是 GitHub Actions 非同步做的，正常就是會有一段時間停在這；而且路線型短連結（`maps/dir/?geocode=`）本來就展不開（CLAUDE.md 已知缺口），不能讓它看起來像壞掉。

**「貼上新連結」的行為（核心）**

```
1. navigator.clipboard.readText()
2. 讀到且是 Google 地圖連結 → 直接覆蓋 mapUrl、清空 addr、回狀態 3、toast「已換成剛剛複製的連結」
3. 讀到但不是地圖連結／剪貼簿空／readText 被拒或不支援
   → 展開手動輸入子狀態（input 帶現值、focus 並整串選取），toast 說明原因
```

- 連結判定：`/^https?:\/\/(maps\.app\.goo\.gl|goo\.gl\/maps|(www\.)?google\.[a-z.]+\/maps)/i`
- 貼到跟現值一模一樣 → 不動資料、toast「跟現在這條一樣，沒有換」（避免白白清掉 addr 再重展開一次）
- **`readText()` 一定要有 fallback**：iOS Safari 會跳系統的「貼上」確認、使用者可能不點；非安全脈絡直接 reject。**沒有 fallback ＝ 這個功能在他手機上有機率整個不能用。**

**手動輸入子狀態**

```
[ https://maps.app.goo.gl/9Yh2…  ] [ 完成 ]   ← input 16px + .m-done 46px
整串已經幫你選起來了，直接長按貼上就會蓋掉舊的。
```

- 進來時 `el.focus(); el.setSelectionRange(0, el.value.length);`
- **iOS 用 `setSelectionRange` 不要用 `select()`**，而且要在 user gesture（按鈕點擊）的同一個 tick 內做，包 `try/catch`
- 按「完成」收回顯示態；值有變就清 `addr`

### A2.4 資料規則（**dev 必守，這幾條錯了會沉默壞掉**）

1. **`mapUrl` 一有變動（含清空）就把 `addr` 設成 `""`**。`addr` 是 `mapUrl` 的衍生值，留著舊地址＝路線鈕會用錯的起訖點。
   現有 `submitStopEdit` 已經有這行（`if(newMap !== String(sp.mapUrl||"")) sp.addr = "";`）——改成即時操作後，**清 addr 的時機要往前移到「按下貼上／✕ 的當下」**，不能只留在 submit。
2. **✕ 清除 ＝ `mapUrl=""` ＋ `addr=""` 兩個都清。**
3. **前端永遠不自己寫 `addr`**（展開只有 server／CI 做得到）。UI 只負責顯示與清空。
4. `transit`（移動）**一個欄位都不准加**——路線連結仍是前端即時算的、不落資料（CLAUDE.md 決策沒變）。這一節只動 stop 的編輯表單。
5. 「新增行程點」表單（`app.js` 約 L1625）用**同一個元件**，開場就是狀態 1。**不要複製第二份 UI。**

### A2.5 CSS 新 class（照 demo 抄；全部用既有色票）

`.mapf` `.mlink`（`.mp`／`.mb`／`.maddr`／`.msub`／`.pending`）`.mlink-acts` `.m-paste` `.m-clear` `.m-manual` `.m-empty` `.m-edit` `.m-done`

要點：

- `.mlink` 用 `.field input` 同一套外觀（`#fbfaf6` 底、`1.5px solid var(--line)`、`border-radius:13px`），看起來還是同一張表單裡的東西
- `.m-paste` ＝ `#fff1ef` 底 ＋ `var(--acc-deep)` 字（跟 A1.4 樣式 B 同一套 tinted 語言）
- **所有按鈕 ≥ 44px**（`.m-paste`／`.m-clear` 用 46px、`.m-manual` 靠 `min-height:44px` 撐開）
- **`.m-edit input` `font-size:16px`**（iOS 鐵律，不准降）
- 貼上圖示（`.m-paste`／`.m-empty` 內）用同一組 stroke 參數的 SVG，path 見 demo 的 `ICO.paste`

### A2.6 dev 檢查點

- [ ] 換連結（剪貼簿有連結時）**1 下**完成
- [ ] `readText` 被拒／不支援 → 自動退回手動輸入，**不會卡死**
- [ ] ✕ 之後 `mapUrl` 與 `addr` 都是空的（存檔後看 md 檔驗證）
- [ ] 貼上新連結後畫面立刻進「地址整理中…」，存檔 → CI 跑完 → 重整後顯示新地址
- [ ] `transit` 的 md 輸出仍然只有 `{id,type,note,stayMinutes}`
- [ ] 新增／編輯兩個入口共用同一個元件
- [ ] `sw.js` cache 版本號 ＋1、`APP_VER` 同步

---

## A3. 要 Benson 拍板的

1. **圖示配色**：A 純線條／**B 淡珊瑚底（UX 推薦）**／C 中性淺底。
2. **灰條左邊那顆 🚶 要不要一起換成線條圖示（依交通方式自動）**：UX 建議換，一致性較好；不換也可以，它算內容。
3. **連結欄方案**：**A 已連結卡＋一鍵貼上（UX 推薦）**／B 欄內 ✕／C 只做聚焦全選。

---

# 附錄 B（v2.7 提案）— 圖示 v2 改版：把那兩顆從「按鈕」降回「記號」

> 起因：v2.6 上線後 Benson 看實機說 **「我要換其他的圖示，現在覺得有點突兀」**。
> 可試玩 demo：**`demo/icons-v2.html`**（單檔、零依賴、雙擊開；`demo/icons-and-maplink.html` 與 `demo/index.html` 都不動）。
> demo 墊的是他**真的 Day 1**（本色季3rd：出發 → 秀水湯包 → 飯捲 → 沐楓商旅 → 陽光劇場 → 夜市，含 4 條移動灰條），不是樣板卡。
> 狀態：**已拍板並實作（v2.7）＝「A 留白 ＋ 圖形組 2（圓潤）」**。落地細節見 `CLAUDE.md` 的「圖示語言（v2.7）」；下面 B2／B3 的 B／C 方向與圖形組 1／3 只留作紀錄，不是現況。

## B1. 診斷：它為什麼「突兀」

先講結論：**Benson 猜的「淡珊瑚底把它變成一顆看得見的按鈕」對了一半。**
「有底色」本身不是原罪——旁邊的 `.cat-pill` 也有底色。真正的問題是**它借了旁邊那顆藥丸的底色語言，把一個工具提到跟內容同一階**，再加上尺寸與貼角的幾何問題。所以**只換顏色不夠，尺寸與位置要一起改**。

在實機 Day 1 上量到的六件事：

1. **視覺重量跟資訊重要性相反。** `.map-btn` 是 44×44 的實心色塊，**是整張卡片上最大的單一元素**（比 16.5px 的標題文字塊還大），但它是卡片上最不重要的東西。
2. **它貼在角上，像一張貼紙。** `margin:-10px -10px -8px auto` 把它推到距卡片上緣 **2px**、右緣 **4px** 的位置——一個 `border-radius:12px` 的方塊塞進 `border-radius:16px` 的白卡角落，兩個幾乎同心但不同的圓角疊在一起，一定會讀成「外面貼上去的」。
3. **它跟 `.cat-pill` 撞語言。** 藥丸是**內容**（「這是美食」，用類別色 10% 底），它是**功能**，但兩個都變成「淡色圓角色塊」並排。那一行於是讀起來像「時間 ・ 美食 ・ 一個什麼都沒說的珊瑚標籤」。
4. **灰條那顆更嚴重。** `.transit-bar` 的設計目的白紙黑字寫著「輕薄一條、不搶版面」，而 38px 的鈕**等於整條的高度**，把那條唯一沒有顏色的元素右端整個染成珊瑚色。
5. **它是行程頁內容區唯一的珊瑚色，而且會重複。** 真實 Day 1 上它出現 5 次（3 個 pin ＋ 2 個 route）、且**出現得不規則**（沒有 `mapUrl`／算不出起訖點的就沒有），沿著右邊形成一條斷斷續續的色點柱，把視線往下拉，卡片內容反而變配角。
6. **同一張卡上有兩顆針。** 地點那行已經有一顆內容型 `📍`，右上角又是一顆描邊大頭針——同一個字講了兩次。
   （另外：`route` 的箭頭是這個 App 唯一的**尖角**——全站都是 99px 藥丸、16~20px 圓角、圓點、`linejoin:round`。尖角本身就吵。）

## B2. 三個方向（demo 上可即時切；四個共用同一份真 Day 1）

| | 做法 | 個性 | 成本 |
|---|---|---|---|
| **現況 v2.6** | `#fff1ef`／`#fbeae7` 底 ＋ `--acc-deep`，44px／38px 方塊 | 對照組 | — |
| **A 留白（大幅減法）★ 推薦** | **底色整個拿掉**、筆畫 1.8→1.6、色 `#a89d8d`（灰條 `#9a9082`）、圖 22→21px（灰條 19→18px） | 最安靜；它不再是按鈕，是卡片角落的一個記號 | 只改 CSS，最小 |
| **B 米灰圓鈕（換家族）** | 底色改 `#f3eee4`（灰條 `#e5ded0`）＝跟 `.tool-btn`／`.drag-handle` 同家族；**方形改圓形**、44→32px（灰條 28px） | 仍看得出可點，但語意從「標籤」變成「工具」 | 只改 CSS，小 |
| **C 貼著內容走（換位置）** | 卡片那顆**搬到名字後面**（`.stop-name` 行尾，inline）；灰條那顆改成右邊一句「**路線 ›**」淡字 | 最像「原本就長在那裡」；`.stop-top` 只剩時間＋類別，乾淨很多 | **要動 `app.js` 的 markup**，中 |

三個方向刻意**各動一個軸**：A 動「有沒有底色」、B 動「屬於哪個家族＋重量」、C 動「位置」。

**每個方向都順手修掉 B1 的幾何問題**：不再貼著卡片的角（A 把 margin 改成 `-11px` 讓圖形右緣落在卡片的 14px padding 線上；B 縮到 32px 圓形、離角 8px／12px；C 直接離開右上角）。

**觸控目標一律還是 44×44**：A 沒動 `.map-btn` 本體尺寸；B 用 `::before{inset:-6px}`（卡片 32+12）／`inset:-8px`（灰條 28+16）外擴；C 用 `::before{inset:-12px}`（19px 圖形 → 43~44）。**灰條總高仍是 38px**，四個方向都沒動 `.transit-bar` 本體。
調整模式（`ui.edit`）四個方向一樣不顯示這兩顆。

## B3. 圖形三組（跟樣式方向正交，可任意配）

他說的是「換**圖示**」，所以除了樣式也給了三組 glyph，demo 上可獨立切換：

| | 地點 | 路線 | 說明 |
|---|---|---|---|
| **1 現行** | 水滴 pin | 起點圓＋轉彎＋箭頭 | v2.6 線上這組 |
| **2 圓潤 ★ 推薦** | **折頁地圖**（三折） | **一條蜿蜒的路**（起點圓 → S 曲線 → 終點圓，**沒有箭頭**） | 全圓角零尖角，跟全站的圓形語言一致；也順手解掉「同卡兩顆針」 |
| **3 幾何** | 定位準心（同心圓＋十字刻） | 導航指標（Google Maps 那個三角） | 最俐落，但準心比較「儀器感」、指標是尖的 |

**兩顆仍然分得出是不同動作**（v2.6 定下的界線）：組 2 是「一張攤開的地圖」vs「一條路」，組 3 是「一個定點」vs「一個方向」，縮到 18px 也不會認錯。
組 2 的代價：路線少了箭頭＝少了「方向」的暗示，靠兩端的起訖圓與 S 形補。若 Benson 覺得方向感不夠，退回組 1 的 route 也可以（樣式與圖形互不綁定）。

## B4. 我推薦：**A 留白 ＋ 圖形組 2**

理由：
- 直接對症 B1 的 1/3/4/5 五條裡的四條——沒有底色就沒有「第二顆藥丸」、沒有右邊那條色點柱、灰條回到全灰。
- **找不到的風險很低**：整張卡本來就可以點，點進去詳細頁還有一顆全寬的「開啟 Google 地圖」；灰條那條也是點了就開編輯。這兩顆是捷徑不是唯一入口，安靜一點的代價他付得起。
- **成本最低**：純 CSS，`app.js` 只換兩段 path（若同時換圖形）。
- 如果試玩後覺得 A 太淡找不到 → **直接跳 B**（同樣純 CSS，一分鐘的事）。C 留給「他覺得右上角就是不該有東西」的情況。

## B5. 被選中之後，lab-dev 要改哪裡

### 共同（不管選哪個方向）
- `public/styles.css`：只動 `.stop-card .map-btn`、`.transit-bar .map-btn`、`.map-btn .ico` 這幾條，**`.map-btn` 本體的 width/height/圓角別亂動**（44px／38px＋`::before inset:-3px` 是既有決策）。
- `public/app.js`：只有換圖形時才動 `ICO.pin` / `ICO.route` 的 path（`demo/icons-v2.html` 的 `GLY` 物件裡可直接抄）。**`ICO.paste` 不要動。**
- **`.map-btn .ico` 在正式版要保留 `display:block`**（demo 為了三組 glyph 疊放才拿掉，別把 demo 的寫法照抄）。
- **demo 的 `sty-*`／`g1 g2 g3`／`.at-name`／`.mb-lbl` 切換死碼一律不要帶進正式版**，只留選中那一組。
- `APP_VER` ＋ `sw.js` 的 `SHELL_CACHE` 與 `DATA_CACHE` **兩個都 +1**（PWA 鐵律）。
- `CLAUDE.md` 的「圖示語言（v2.6）」那段改寫成 v2.7：**把「配色＝B 淡珊瑚底（Benson 拍板）」換掉，並註明是他看實機後推翻的**，別讓下一個人當成誤改修回去。
- **內容型 emoji 一顆都不准動**（類別、封面、tab、灰條左邊的 🚶、地點行的 📍、詳細列前的那排、`✕ ☰ ✎ ＋`）。

### 選 A 時（純 CSS）
```css
.map-btn .ico{width:21px; height:21px; display:block; fill:none; stroke:currentColor;
  stroke-width:1.6; stroke-linecap:round; stroke-linejoin:round;}
.stop-card .map-btn{color:#a89d8d; background:none; margin:-10px -11px -8px auto;}
.stop-card .map-btn:active{background:#f4efe5;}
.transit-bar .map-btn{... margin:-7px -7px -7px auto; color:#9a9082; background:none;}
.transit-bar .map-btn:active{background:#e6dfd1;}
.transit-bar .map-btn .ico{width:18px; height:18px; stroke-width:1.7;}
```
（`.map-btn` 的 `font-size:20px`／`17px` 早就沒作用，可順手刪。）

### 選 B 時（純 CSS）
照 demo 的 `.sty-b` 那一段抄，重點：卡片 32px 圓形 ＋ `::before{inset:-6px}`，灰條 28px 圓形 ＋ `::before{inset:-8px}`（**原本那條 `inset:-3px` 要一起改成 -8px**，否則命中區只有 34px）。

### 選 C 時（要動 markup）
- `app.js` `viewPlan()`：把卡片的 `.map-btn` 從 `.stop-top` 移到 `.stop-name` 裡（接在 `esc(sp.title)` 後面），**`onclick="event.stopPropagation()"` 一定要留**。
- 灰條那顆加一個 `<span class="mb-lbl">路線</span>`，CSS 用 `::after{content:" ›"}` 補箭頭。
- QA 要驗：長標題換行時圖示跟在最後一行、命中區 ≥44、**點名字後面那顆不會誤開詳細 sheet**。
- 已知小代價：名字後面的 44px 命中區會蓋到地點那行右側的空白（點錯只是開地圖，不會壞資料）。

## B6. 要 Benson 拍板的

1. **樣式方向**：現況／**A 留白（推薦）**／B 米灰圓鈕／C 貼著內容走。
2. **圖形組**：1 現行／**2 圓潤（推薦）**／3 幾何。（跟 1 互相獨立，可任意配。）

---

# 附錄 C（v2.8）— 行程時間對不起來的提示 ＋ 重新排

> **這份規格是事後補寫的。** lab-ux 做完 demo 之後、還沒寫規格就被額度上限打斷，
> 唯一的規格來源是 **`demo/reschedule.html`**（視覺與行為都以它為準）；這一節由 lab-dev 讀通那份 demo 之後補寫，
> 含**為什麼這樣做**與**跟 demo 不一樣的地方**，讓下一個人看得懂。
> 狀態：**已實作（v2.8）**。落地細節與界線見 `CLAUDE.md` 的「銜接提示與重新排（v2.8）」。

## C1. 問題：連鎖平移修不好的那 35 分

v1.6／v2.5 的**連鎖平移**是「後面全部加減同樣的分鐘數」。它**保留原本的相對關係，包括原本就錯的那一段**。

Benson 真實的 Day 1（`data/trips/mrxovz2t-本色季3rd.md`）：

| | 他填的 | 照停留＋移動走，實際到得了 |
|---|---|---|
| 好難吃養生紫米海苔飯捲 | 10:40，停 240 分 ⇒ 14:40 離開 | — |
| 開車 | 40 分 | ⇒ **15:20** |
| 沐楓商旅 | **14:45** | **晚 35 分** |

他改了三次開車時間，每次平移都正確執行，但**那 35 分的缺口被原封不動搬著走**，永遠修不好。
缺口是更早「新增／拖曳」時造成的——**那兩條路徑當時完全不會推動後面**。

所以 v2.8 補的是 App 從來沒有算過的另一半：**「這一站其實幾點才到得了」**。

## C2. 演算法（demo 的精華，不可以做簡化版）

三支函式都在 `public/app.js`，跟 v1.6 的 `shiftAfter`／`stopShiftDelta` 並存、互不取代。

### `analyzeDay(list)` — 走一遍這一天
對每個行程點算兩個數字：
- `planned`＝他填的時間；`arrive`＝**照前面的停留＋移動走，走得到的時間**（cursor）。
- `gap = arrive − planned`：**>0 ＝來不及、<0 ＝空檔、null ＝算不出來**（沒填時間，或前面沒有任何基準）。
- 走完一站之後 **cursor 以「他填的時間」重新對錶**（跟 `nextTimeGuess` 同一條規則：手填的最大）——
  否則第一個缺口會污染整天，後面每一站都被判成來不及。
- **跨午夜**：cursor 可以超過 1440，`liftTime()` 把他填的鐘面時刻抬到 cursor 所在的那一圈
  （宵夜 `01:45` 才不會被當成「昨天凌晨」而誤報成晚 22 小時）。

### `lateSet(list)` — 只標負的差
`gap >= GAP_MIN(5)` 才收。**正的差（空檔）是他刻意留的緩衝，絕對不標。**
（demo 裡那個「連空檔也標出來」的開關是**對照組**，證明「全標」會讓提示變成常態、失去提醒的意義，**沒有做進正式版**。）
5 分鐘的門檻：真實移動誤差在這個量級，低於它標出來只是噪音。

### `replanDay(list, startIdx)` — 重新排
規則只有一條：**`新時間 = max(他填的時間, 走得到的時間)`，只往後推、不往前拉。**

- 它**不是**「後面全部往後推 N 分」。**只有來不及的那幾筆會被挪**；
- **撞到他刻意留的空檔就被吸收掉，後面就不用再推**——某一站原本就晚於「走得到的時間」（空檔夠大）時，
  它維持原時間，連鎖到此為止。這正是跟連鎖平移最大的差別。
- `startIdx` 之前的行程點一律不動（只當對錶基準），所以「從某一站起」與「整天重排」共用同一支，只差 `startIdx`。
- **刻意不做「壓實模式」**（把所有空檔壓掉重排）：v1.6 已拍板「重算會把他刻意留的空檔壓掉」，那條界線在 v2.8 沒有變。

### `bufferDiff(list, newT)` — 哪段空檔被吃掉多少
拿同一支 `analyzeDay` 跑「換過時間的副本」，比對原本 `gap<0` 的那幾筆。
**這是最容易讓他不爽的副作用，預覽一定要先講。**

### 用他真實的 Day 1 對答案（實測，不是推算）

```
沐楓商旅      14:45 → 15:20  (+35)     ← 唯一被標「銜接不上」的一處
桃園陽光劇場  15:45 → 16:20  (+35)     ← 連鎖到它
夜市 ( 宵夜 ) 01:45 不動                ← 被空檔吸收，連鎖停在這裡
夜市前面的空檔  1 小時 → 25 分
其他 4 筆時間不動；套用後 lateSet = 0，再跑一次 replan 的變動數 = 0（冪等）
對照組：走 v1.6 的 shiftAfter 平移，接不上的數量仍然是 1（缺口被搬著走）＝問題本身
```

## C3. 三個元件（層級宣告——v2.7 的教訓：新元素要先想清楚它是哪一層）

| 元件 | 是哪一層 | 所以 |
|---|---|---|
| `.gap-note` 銜接條 | 「**這張卡的狀態**」 | 無底色、hairline、12px，只借卡片既有的**分隔線**語言，**不借藥丸的底色語言** |
| rail `.ln.to-late` | 「**兩站之間的狀態**」 | 零高度成本、只換顏色，捲動時一眼掃得到位置 |
| `.fix-bar` 重排入口 | 「**工具**」 | 才給底色、才給 ≥54px 的觸控目標 |

- **配色＝琥珀**：沿用 App 既有的「有新版本」語言（`.ver-btn.hot`／`.ver-alert`），
  **刻意不新開一套色**，也**刻意不用 `--bad`**（那是「錯誤／刪除」的語言）。**語氣是提醒不是報錯。**
- **銜接條文案**（Benson 拍板的 A 案）：`● 晚 35 分・15:20 才到得了　　重新排 ›`，位置在卡片最上面一行。
- **Day 層級摘要**（`.fix-bar`，只有真的接不上才長出來）：
  `● 這一天有 N 處銜接不上 / 沐楓商旅 晚 35 分（，還有 M 處）　[重新排 ›]`。
- **調整模式（`ui.edit`）不顯示銜接條**：它會改變卡片高度、干擾拖曳的讓位計算。
  **`.fix-bar` 照常顯示**（它在 timeline 之外，不影響拖曳）。

## C4. 重新排的流程（**一定要先預覽**）

`銜接條／fix-bar → 預覽 sheet →「就這樣排」才套用`。**這是他的真行程，不可以自作主張改。**

預覽方式＝**差異清單**（Benson 拍板；demo 裡的「就地預覽」候選沒有做進正式版）：
1. **為什麼**（`.rp-why`）：某站排在幾點、但幾點才到得了；並保證「你刻意留的空檔會留著」。
2. **範圍**（`.rp-scope`，兩顆）：**從「某站」起** / **整天重排**。
3. **會變動的 N 筆**（`.rp-row`）：`名稱　14:45 → 15:20　+35`。
4. **其他 N 筆時間不動。**
5. **你留的空檔**（`.rp-buf`）：`夜市 前面的空檔 1 小時 → 25 分`＋「延誤被這段空檔吸收掉了，後面不用整串往後推」。
6. **兩顆鈕**：`先不要`（＝`closeSheet()`，**什麼都不動、連寫入都不會發生**）／`就這樣排`。

## C5. 新增之後也要推

- `submitStop`／`submitTransit` 加完之後呼叫**既有的** `shiftAfter(list, at, push)`，
  `push`＝新的停留（或移動時間）。**沿用既有的 `shiftAfter` 與 toast，沒有另寫一份。**
- ⚠️ **已知限制**：目前新增一律**加在最後**（沒有「插在中間」的入口），所以實務上 `moved` 幾乎都是 0；
  這條路徑是**為了未來的「插在中間」先接好**，也讓「新增」不再是缺口的來源。
  他真正製造缺口的動作是「加在最後 → 拖進中間」，而**拖曳排序後刻意不自動動**（意圖不明確）——
  排序後如果變成來不及，**銜接條會自己出現**（`render()` 每次都重算），由他決定要不要重排。
- 加完之後如果這一天接不上，toast **升級成可點的**（`toast(msg, false, {label:"重新排", fn})`）直接帶去重排；
  toast 的動作鈕是 v2.8 給既有 toast 加的**選配第三參數**，不帶就跟舊版一模一樣。

## C6. 跟 demo 不一樣的地方（都有理由，別當誤改改回去）

1. **`.gap-note::before` 的命中區從 `top:-9px` 改成 `top:-18px`。**
   demo 的註解寫「外擴回 44px」，但實測那組數值只有 **35.2px**（視覺高 26.2 ＋ 9 ＋ 1）。
   **往下不能擴**（會蓋到 `.stop-top` 那排的地圖鈕與時間，把「看詳細」偷成「重新排」），所以往上補到 -18px，**實測 44.2px**。
2. **新增 `.stop-card .map-btn{position:relative}`。**
   銜接條是 `position:relative`，而地圖鈕有 `margin-top:-10px`（會往上探進銜接條那一行）⇒ 被銜接條疊住，
   **實測命中高從 44.0 被壓成 41.5px**（v2.4/v2.7 訂死 44）。讓地圖鈕自己也 positioned 就贏回堆疊順序，
   **純堆疊、零版面變化，實測回到 44.0px**。
3. **「連空檔也標出來」的開關沒有做**（demo 專用對照組）。
4. **「就地預覽」候選沒有做**（Benson 拍板用差異清單）。
5. **demo 的 `toast` 動作鈕 `min-height:34px` 改成 44px**（專案的觸控目標鐵律不讓）。

## C7. QA 檢查點

- 用**他真實的 Day 1** 對 C2 那組數字（沐楓 +35、陽光劇場 +35、宵夜不動、空檔 1 小時→25 分）。
- **只標負差**：夜市那筆早到 1 小時，畫面上不可以有任何提示。
- 預覽 →「先不要」：`itinerary` 的 JSON 必須一個字元都沒變、`persistTrip` 一次都沒被呼叫。
- 觸控目標：`.gap-note` ≥44（含 `::before`）、`.fix-bar` ≥54、**同一張卡的地圖鈕仍然是 44**。
- 既有功能：v1.6/v2.5 連鎖平移、v1.5 起訖時間、v1.7 停留欄、v2.4 路線連結、v2.6 地址欄、鑰匙圈解鎖。

---

# 附錄 D（v2.9 提案）— 打包清單改版：包（容器）＋ 拖曳歸屬 ＋ 兩種新增

> demo：`demo/packing.html`（單檔、雙擊即開、手機優先）。**不要覆蓋 `demo/reschedule.html`。**
> 資料底是 Benson 真實旅程「本色季3rd」的打包清單，補到 24 筆＋三個真的裝了東西的包。

## D1. 診斷：為什麼「整體流程不方便」

Benson 抱怨了三件事（不能編輯／不能改歸屬／新增太陽春），但**病根只有一個**：

> **這一筆東西的「歸屬」，從頭到尾沒有任何地方可以改。**

拆開來看：

1. **完全沒有「編輯」這個動作。** 資料是 `{id,text,done,zone}`，每一列只有 `勾／文字／✕`。
   打錯字、想改區、想改順序 —— **唯一路徑是刪掉重打**，而且重打之前還得先把上面的 segmented 切到對的區。
   一個 15 秒的念頭要花 4 個動作，這就是「不方便」的主體。
2. **segmented 一個控制項兼了兩份工作，而且看不出來在做哪一份。**
   它長得像「切換檢視」（全站最像 tab 的元件），實際上只決定「新增到哪一區」，
   下面兩區永遠同時全部列出 ⇒ **點下去畫面幾乎沒變化**（只有輸入框的 placeholder 換字）。
   使用者第一個反應是「壞了？」，而他真正想做的「把這樣東西改成隨身」它又幫不上忙。
3. **他早就在既有框架下自己變通了 —— 那是需求訊號，不是滿足。**
   他的真實資料裡已經有 `盥洗包`、`防水包`，內建模板裡還有 `溫泉小包（髮圈/髮夾）`。
   他一直用「一行字」代表一個包，**裡面有什麼只能靠記憶**。
4. **「新增太陽春」的真正問題不是長得醜，是位置。**
   輸入列釘在最上面，而要加的東西可能屬於下面第二區裡的某個包；
   加完之後那一筆出現在畫面下方，**輸入框旁邊沒有任何回饋**。

所以這一版的主軸是：**把「歸屬」變成一等公民**（可看、可拖、可選、可改），
而不是「加一個資料夾功能」。

## D2. 資訊結構（定案建議）

- **三層、到此為止**：區（zone）→ 包（bag）→ 物品。包不能放進包。
- **包同時是容器也是一件物品**：它有自己的勾（＝整包丟進行李箱了），也可以展開編輯內容。
- **各勾各的**（Benson 拍板）：勾包 ⇄ 勾包內物品，**兩件事完全不連動**。
  - 衍生規則：包被勾起來時**自動收合**（丟進去了就不用看裡面），但還是點得開。
  - ⚠️ **視覺上也不准連動**：包被勾＝只有「包」那一列變灰＋刪除線，
    裡面的東西一個字都不能被劃掉（demo 施工時就踩到這條，見 D9）。
- **兩種進度，分母不同**（一定要寫進 dev 工單，不然數字會對不起來）：
  - 區的「已打包 x / y」：分母＝**這一區的頂層項目**（包算 1 件），包內物品**不計入**。
    語意＝「行李箱裝好沒」。
  - 包的「已裝 x / y」：分母＝這個包裡的物品。語意＝「這個包裝好沒」。

## D3. 版面（減法優先）

現有畫面已經有 segmented(50) ＋ 輸入列(50) ＋ 模板兩顆(44) ＋ 區標題，再加縮排／摺疊／把手會爆。
所以**先砍再加**：

| 動作 | 內容 | 省下 |
|---|---|---|
| 砍 | segmented（職責已被「就地新增」取代，見 D5） | 一列 50px |
| 併 | 頂部輸入列 ＋ 模板兩顆 → **一列**：`[＋ 新增物品（珊瑚，主要）] [📦 模板]` | 一列 44px |
| 換 | 每列的 `✕` → **拖曳把手 `☰`**（刪除移進編輯／長按選單，順便防誤刪） | 控制項數量不變 |
| 加值 | 區標題右邊的「已打包 3 / 8」**變成可點**＝只看這一區沒打包的 | 零新增元素換到篩選 |

**層級靠「同一張卡」不靠縮排**（這一版最重要的視覺決策）：

- 一般物品＝一張白卡（＝現有 `.pack-item` 的尺寸／圓角／陰影，沒動）。
- **包＝同一張白卡長高**：上半是包自己那一列，下半 `.pk-inner` 是內容，
  用 `border-top` hairline ＋ **左側 2px rail** 表達「在裡面」，內縮只有 30px。
- 好處：① 手機水平空間幾乎沒被吃掉（實測 375px 下包內文字仍有 **219px**，長名稱不折行）；
  ② 「一張卡＝一個實體」剛好就是「包是一個東西」這件事本身。

**只在需要時出現的東西**：包內容（要展開）、「這個包的設定」列（只在展開時）、
就地新增的輸入框（點了才變）、拖曳中的落點提示（放開就消失）。

## D4. 難題一：手機上的拖拉

### D4.1 要不要進「調整」模式 → **不要，把手常駐**（推薦，理由如下）

- 行程分頁要進調整模式，是因為**卡片本身就是主要點擊目標**（點卡片＝看詳細），
  常駐把手會跟內容搶空間；而且行程排序是「規劃時做一次」的低頻動作。
- 打包相反：主要動作是「勾」，每一列右側本來就是空的，把手不搶任何東西；
  而移動是**高頻且零散**（想到就改）。要他先按「調整」→ 拖 → 按「完成」＝三個動作換一次移動。
- 而且這一版**已經把 `✕` 拿掉了**，把手是補上那個位置，**每列的控制項數量前後一樣多**。
- demo 可即時切成 B 案（比照行程要按「調整」）給 Benson 比。

### D4.2 手感沿用行程那一套（**別另創**）

pointer events ／**只有把手可拖**（`touch-action:none`）／`setPointerCapture` ／
`preventDefault` ／邊緣自動捲動 **TH=80、SPEED=9 的 rAF 迴圈**（跟 `dragAutoScroll` 同一組數字）。

**跟行程唯一的差別，是落點的算法**（行程只有「排序」，打包還有「進包／出包／換區」）：

- 行程是「index 位移＋讓位動畫」，座標用 page 座標當基準（那條規則是為了那個算法）。
- 打包改成 **slot 制**：每個插入點在 DOM 裡放一個零高度的錨
  `<i class="pk-slot" data-z data-b data-ref>`（**要 `display:block` 才量得到寬度**），
  **每次 pointermove 重算 `getBoundingClientRect`**。
  因為每一幀都重量、沒有「開始時的基準」，所以用 client 座標是正確的（自動捲動後 rect 自己就更新了）。
  **dev 若改回 transform 位移法，就必須回到 page 座標。** 這條要寫進註解，不然下一個人會改錯。
- slot 帶 `data-ref`＝「插在哪一筆之前」（不是 index）。用 index 會在移除來源之後失準。
- slot 制的紅利：跨區、進包、出包、包內排序、包本身排序 **全部是同一個機制**，不用寫五套。

### D4.3 「拖進包裡」vs「拖到包旁邊」怎麼分（這題最難的一點）

**判定**（拖的東西不是包時才啟用；包不能進包）：

- 手指 Y 落在某個包的**包頭那一列的中間 60%** → **進包**
- 落在上緣／下緣各 20% → 退回一般插入點（排在它前／後）

**提示要三個一起上，缺一個都不夠**（demo 實測截圖驗過）：

1. **頂部固定橫條**（珊瑚底、白字）：`→ 放進 📦 盥洗包（🧳 行李托運）` / `→ 排到 🧳 行李托運`。
   固定在畫面頂端，**手指擋不到、不會抖、永遠讀得到**。這是主要訊號。
2. **進包**：整張包卡 `box-shadow: 0 0 0 2.5px var(--acc)` ＋ 包頭淡珊瑚底 `#fff1ef`。
3. **排序**：珊瑚色 3px 插入線 ＋ 左端圓點 ＋ **右端目的地藥丸**（`🧳 行李托運` / `📦 盥洗包 裡`）。
   插入線是 `position:absolute` 的**疊層、零高度**（不是插進去撐開間隙）——
   撐開間隙會讓所有 slot 位置在手指底下跳動、來回抖。

⚠️ **兩個 demo 施工時實測抓到、dev 必須照做的細節**：

- **浮起來的那張卡要「浮在手指上方」，不能壓在手指底下。**
  第一版壓在底下，截圖一看**它剛好完整蓋住你正要放進去的那個包**，高亮等於沒做。
  現在：寬度 `min(卡寬×0.72, 250)`、右緣貼手指右邊 22px、**底緣在手指上方 14px**，
  頂端 clamp 到 52px（不被頂部橫條吃掉）。
- **被拿起來的那一列用 `visibility:hidden` 不能用 `display:none`。**
  用 `display:none` 時，按下去的那一瞬間下面的東西整個往上跳一列 —— **實測位移 66px**，
  手指底下的落點當場換人（demo 第一版就因此把東西丟到隔壁去）。
  改成 `visibility:hidden` 後**實測位移 0**。拖整個包時才額外把 `.pk-inner` 收掉（否則留一個大洞）。

### D4.4 拖拉**不可以是唯一的路**（單手／清單很長／不想拖）

1. **點名字 → 編輯 sheet → 「放在哪」**：把所有目的地攤平成一份可點清單
   （`🎒 隨身` / `↳ 📦 3C 小包` / `🧳 行李托運` / `↳ 📦 盥洗包` / `↳ 📦 防水包`），
   點一下就換位置。包本身只列得到區（只允許兩層）。
2. **長按任何一列（450ms）→ 動作選單**：改名字／換位置、打開看裡面、標成已打包、複製一份、刪掉。
   長按是隱藏手勢，所以**不當唯一入口**，只當捷徑。
   ⚠️ 長按開了選單之後，那一下的 `click` 必須吞掉（capture 階段 `preventDefault`＋`stopPropagation`），
   否則會順便勾起來／開編輯。

## D5. 難題二：「快速加」與「彈窗設定」不是二選一

**分工不是「加什麼」，是「加在哪裡」：**

| | 就地快速加（主力） | ＋ 新增物品（彈窗） |
|---|---|---|
| 入口 | **每個容器自己的底部**都有一條虛線「＋ 加東西到『🎒 隨身』／『盥洗包』」 | 頂部珊瑚色主按鈕 |
| 為什麼 | 打包時人是「想到什麼打什麼」，**每加一樣都跳彈窗會更慢更煩** | 建立新的包／加到現在看不到的地方／一次設定清楚 |
| 關鍵 | **「加到哪」由輸入框長在哪裡決定，不用選** —— segmented 想做卻做不到的事 | 三個欄位就好：名稱、這是一個包（開關）、放在哪 |
| 行為 | Enter 連續加，輸入框留在原位、**focus 不放掉**（實測連加兩筆都留著） | 存檔後關閉 |

**一句話判準：快速加負責「量」，彈窗負責「結構」，模板負責「一整套」。**

新增與編輯**共用同一張 sheet**（別複製第二份 UI；跟 v2.6 地圖欄的決策一致）。

## D6. 模板也要能有包

- 模板項目加兩個選填欄位：`kind:"bag"`（這一筆是包）、`bag:"<包的 text>"`（屬於哪個包）。
  **模板刻意用包的名字而不是 id** —— 模板檔本來就沒有 id、是人可以手打的小清單。
- **帶入的合併規則**（比舊的「同名跳過」多一層）：
  - 模板裡的包，**旅程裡已經有同名的包 → 不新增第二個**，把缺的東西補進他原本那個包。
  - 一般項目的同名判定要**連容器一起比**（`text` 相同**且**在同一個包裡才算重複）——
    「常備藥」放在盥洗包裡跟放在行李箱底層是兩件事。
  - 帶入後 toast：`帶入 7 樣，1 個包直接合併`。

## D7. 資料格式建議（前後端各一套 mirror parser，改要一起改）

**推薦：維持「平的一行一筆」，只加兩個選填欄位。**

```
## 打包
- {"id":"x1","text":"盥洗包","done":false,"zone":"checked","kind":"bag"}
- {"id":"x2","text":"牙刷牙膏","done":true,"zone":"checked","bag":"x1"}
- {"id":"x3","text":"摺疊傘","done":false,"zone":"checked"}
```

- `kind:"bag"`：這一筆是一個包。**缺值＝一般物品**（舊資料零遷移）。
- `bag:"<父包 id>"`：在哪個包裡。**缺值＝直接放在區裡**（舊資料零遷移）。
- key 順序固定 `id,text,done,zone,kind,bag`，**空值不寫**（跟全站 serializer 一致）。

**為什麼不用巢狀 JSON（`{...,"items":[...]}`）**：一行會變很長、git diff 讀不了、
衝突機率上升，而且前後端 parser **和所有既有的 `t.packing.filter(...)` 邏輯都要重寫**。
平的清單＋兩個欄位，`cleanPackItem` 只多兩行，其他程式幾乎不用動 —— 這是最便宜的一條路。
（也不建議把包獨立成 `## 包` 一段：兩段之間的排序要同步，更痛。）

**normalize 規則（前後端都要，且要冪等）**：

1. `bag` 指向不存在的 id，或指向另一個包中的包（三層）→ **降級成頂層**（`bag=""`）。
   跟類別的 `other` 是同一個哲學：**壞掉的參照要有 fallback，不能整份炸掉**。
2. `kind==="bag"` 的項目**強制 `bag=""`**（包不能在包裡）。
3. **包內物品的 `zone` 不是真值來源** —— serializer 寫入前一律同步成父包的 zone。
   （一個數值不可以兼兩份工作；否則「包在行李、內容標隨身」這種矛盾狀態會慢慢長出來。）
4. serializer 輸出時，**包的小孩緊跟在包後面**（parser 不依賴這個順序，但檔案要人讀得懂）。

**模板檔 `data/templates/<id>.md`**：`{text,zone}` → `{text,zone,kind?,bag?}`，同樣是選填、舊模板不用動。

## D8. dev 要改哪裡

1. **`public/app.js`**
   - `cleanPackItem`：加 `kind`／`bag` 兩個欄位（空值不寫）＋ D7 的四條 normalize。
   - `viewPack`：整段重寫（照 demo）。砍 segmented、頂部工具列併成一列、
     `sectionHtml`／`rowHtml`／`bagHtml`／就地新增／slot 錨。
   - 新增：`toggleBag`／`toggleFilter`／`moveTo(item,zone,bagId,refId)`／`tailIdx`／
     `openItemSheet`（新增與編輯共用）／`openActions`（長按）。
   - 拖曳：**新開一組 `packDrag*`，不要去改 `dragStart/dragMove/dragEnd`**（那是行程的，
     兩者算法不同，混在一起兩邊都會壞）。自動捲動的常數共用。
   - `applyTemplate`：改成 D6 的合併規則。
   - 刪除包時：**裡面的東西倒出來留在同一區**，不要跟著消失（要有 toast 講清楚）。
2. **`server.js`** 的 mirror parser／serializer：同 `cleanPackItem` 的兩個欄位＋同一套 normalize。
   ⚠️ **兩邊要一起改**，只改一邊＝手機寫進去的 `bag` 會被電腦端洗掉（同一條規則活在兩套實作裡的老坑）。
3. **`public/styles.css`**：新增 `.pk-*` 一組（照 demo 抄，全部用既有色票，沒有新開顏色）。
   舊的 `.pack-item`／`.pack-main`／`.pack-add`／`.tpl-row` 保留給模板編輯 sheet。
   ⚠️ **`.pk-bag .t{}` 這種寫法會掃進包「裡面」每一列**（跟 `#kr-full .kr-id span` 同一種病）——
   包頭專用的規則一律綁 `.pk-bag .pk-head-row .t{}`。
   ⚠️ **包裡面的列一定要同時帶 `pk-row` 這個 class**：所有「已打包」的樣式都掛在 `.pk-row.done` 上，
   少了它 → 包裡面勾起來畫面完全沒反應（**資料是對的，所以功能測試抓不到，只有截圖看得見**）。
4. **`APP_VER` ＋ `sw.js` 的 `SHELL_CACHE`／`DATA_CACHE` 兩個版本號一起 +1**
   （版本號要「比已經上線的那個大」，動手前先看檔案現況與 `git log`）。

## D9. QA 檢查點（都要附實測值，不准用字級／推算）

- **觸控目標**：`#app` 裡**全掃**（`querySelectorAll('button,input,a')`）逐一驗 ≥44px，
  **不准列白名單**；掃到少於 20 個就判定尺壞了。（demo 實測：掃 49 個、0 個不合格。）
- **表單**：所有 input `font-size` 必須是 16px。（demo 實測：16px。）
- **拖曳**：
  - 拿起來的瞬間，下面某一列的 `getBoundingClientRect().top` **位移必須是 0**。（demo 實測：0。）
  - 壓在包頭中間 → `.drop-into` 出現且插入線消失；移到包頭上緣 → 插入線出現且 `.drop-into` 消失。
  - **測之前一定要先把來源與目標都捲進畫面**（`scrollIntoView`）並斷言座標落在 0~812 之間 ——
    座標在畫面外時事件打不到任何東西，會長得像「拖曳功能壞了」。（這一輪就先誤判過一次。）
  - 量測前先把 device metrics 鎖成 **375×812**；headless 預設視窗寬度不是 375（實測 504），
    量到的寬度會整組是假的。
- **各勾各的**：勾包 → 包內每一筆的 `done` 一個都不能變（**驗資料**），
  **而且**包內每一列的 `text-decoration-line` 不可以出現 `line-through`（**驗畫面**）。
  這兩件事要分開驗，理由見 D8 的兩條 ⚠️。
- **已打包 x/y**：分母＝頂層項目數（包算 1），不是全部物品數。
- **模板**：帶入含包的模板兩次，第二次必須是「0 樣、1 個包合併」（冪等）。
- **既有功能不能壞**：行程分頁的拖曳排序（那是另一套）、v2.8 銜接條、鑰匙圈解鎖。

## D10. 要 Benson 拍板的（demo 的「⚙ DEMO 設定」可以即時切）

1. **點一列會發生什麼事** —— A 點框＝勾／點字＝編輯（**推薦**，跟提醒事項一致，編輯才有地方放）
   vs B 點整條＝勾（維持現在的肌肉記憶，編輯只走長按）。
2. **上面那條 🎒／🧳 切換要不要留** —— A 拿掉（**推薦**，職責已被就地新增取代、省一列）
   vs B 改成真的只看一區（清單變短）vs C 維持現在。
3. **拖曳把手要不要常駐** —— A 一直都在（**推薦**，理由見 D4.1）vs B 比照行程要先按「調整」。
4. （順帶）**包被勾起來要不要自動收合** —— demo 目前是「會收合」。

---

# 附錄 E（v2 修正提案）— 打包清單：「醜」的診斷 ＋ 拖曳誤觸

> demo：`demo/packing-v2.html`（單檔、雙擊即開、手機優先）。**不要覆蓋 `demo/packing.html`。**
> demo 裡「⚙ DEMO 設定」最上面有兩顆大鈕，可以在 **「現在線上的 v2.9」⇄「v2 新版」** 之間一鍵切換對照。
> 資料底＝Benson 真實旅程「本色季3rd」的 22 筆打包清單（原字不動），再把「盥洗包／防水包」變成真的包。
> 本附錄所有數字都是 **headless Chrome 鎖 375×812 實測**，不是推算。

Benson 的回報：①「ui 有點醜」②「現在拖行李很容易不小心按到」。
**這兩件事是同一個東西造成的**，所以放在同一份提案裡。

## E1. 診斷：為什麼「醜」

病根一句話：

> **打包清單借用了「一張卡＝一個實體」的語言，去排一份 22 筆的清單。**

這是這個 App 第五次踩到「某元件借了別人的視覺語言」。三個症狀都從這裡長出來：

### 症狀① 19 張浮卡 ＝ 層級整個扁掉，而且畫面很碎（主症）

實測（375×812，Benson 真實清單）：

| | v2.9 現況 | 說明 |
|---|---|---|
| 有陰影的元素 | **21 個** | 19 張卡各一道，加上頂部兩顆鈕 |
| 卡間空隙 | 每張 `margin-bottom:10px` | 19 張 ＝ **190px 純空隙** |
| 頁面總高（包收合） | **1930px** | ＝ 2.4 個螢幕 |

「包」跟一支「電動牙刷」**視覺重量完全一樣**——同樣的白底、同樣的 16px 圓角、同樣的 `--shadow`。
包是容器這件事，只有在你把它**展開**之後才看得出來；收起來的時候它就是清單裡的一張卡。
D3 當初訂「包＝同一張白卡長高」是為了讓包看起來像一個實體，
但那條規則**被套到 19 筆同質小東西身上**，結果是誰都不像實體。

而且 **App 自己處理長清單的語言根本不是這個**：
首頁「回憶」是 `.mem-card`（**一張卡**）＋ 內部 `border-top:1px solid #f2ecdf` 的 `.mem-row`。
`.stop-card` / `.exp-item` 那種「每筆一張浮卡」是給**少量、每筆都是一個實體**的東西用的。
打包是全 App 筆數最多的清單，卻挑了最碎的那一套語言。

### 症狀② 右緣一條 ☰ 柱 —— 這正是 v2.7 才剛修掉的病，原樣復發

實測：**28 個把手全部落在 x=309~357 這條 48px 寬的縱帶上，縱向覆蓋清單 70%。**

`styles.css` 裡 v2.7 的註解自己寫著（`.map-btn` 那一段）：

> 「44px 實心色塊是整張卡最大的元素、貼在卡角像貼紙、**沿右邊重複出現形成一條色點柱**」

v2.9 把同一件事在打包分頁重做了一次，只是**從色塊換成符號**（19px 的 `☰`，`#cbc0ad`）。
而且 `☰` 在這個 App 只有 `.tool-btn` / `.drag-handle` 用，**那是「調整模式」才出現的工具**；
常駐等於把「編輯模式的工具」提到跟內容同一階。

**這條柱子同時就是問題②誤觸的來源** —— 所以「醜」跟「誤按」是同一件事，見 E2。

### 症狀③ 頂端那顆珊瑚實心鈕，把視覺焦點從內容搶走

實測：`.pk-new` 是 **256×50 ＝ 12,820px²**，是整頁**最大的飽和色塊**，
是一顆打勾完成圈（27×27 ＝ 729px²）的 **17.6 倍**。

珊瑚在這個 App 的語意是**「完成／進行中的狀態」**（`.pk-box.done`、`.edit-toggle.on`、`.count-chip`）
與**「sheet 裡的送出」**（`.btn-primary`）。把它常駐成頁面頂端的入口，等於：

- 用「送出」的語言做一個平常在用的入口；
- 跟下面「勾完會變珊瑚」的進度回饋**搶同一個顏色** ⇒ 你打勾了 8 樣，畫面上最亮的還是那顆按鈕。

而且按 **D5 自己的規格**，這顆是「負責結構」的**次要**入口（主力是每區底部的就地新增）——
**視覺權重跟使用頻率是反的。**

### 症狀④（小）區標題跟內容爭

實測 `.pack-head .t` ＝ `15px / 800 / rgb(43,38,32)`（純 ink），
而 App 標準的區標題 `.sec-title` 是 `14px / 800 / var(--muted)`。
打包的「🎒 隨身（上飛機帶著）」跟卡片內文一樣黑一樣重，標題沒有退到背景。

## E2. 誤觸：根因與解法

### 根因（已量化）

```css
.pk-grip{ width:44px; min-height:56px; touch-action:none; }
```
```html
onpointerdown="packDragStart(event, id)"   <!-- 一碰就進入拖曳 -->
```

- 把手縱帶 **x=309~357**（螢幕 375，離右緣只有 18px）＝**拇指滑清單的路徑**；
- 縱向覆蓋清單 **70%**，而 `touch-action:none` 讓這 70% **完全不捲動**；
- `pointerdown` **沒有位移門檻、沒有時間門檻**，手指一碰就開始拖。

**實測負控組**（demo 切到「B 一碰就拖」，模擬拇指沿右緣上滑 40px）：**開始拖曳 ＝ 會誤觸**。
這條負控組是量測工具的自證：如果連現況都測不出誤觸，代表尺壞了、後面的綠燈都不算。

### 解法：`pointerdown` 不等於「開始拖」，改成「先觀察，後接管」

**不回頭去做「進調整模式才能拖」** —— D4.1 的理由仍然成立（打包的移動是高頻零散、右側本來就是空的），
那會讓每次移動多兩個動作。真正要修的是**觸發條件**，不是**有沒有把手**。

```
pointerdown → 只進入「待命」：不 preventDefault、不 capture、touch-action:pan-y
  ├─ 垂直位移 > V_ESC = 8px    → 放棄（你在捲動）。因為是 pan-y，瀏覽器早就在捲了，不會頓一下
  ├─ 橫向位移 > H_ARM = 12px   → 進入拖曳（橫向不可能是捲動 ＝ 意圖明確）
  └─ 原地不動滿 T_HOLD = 220ms → 進入拖曳
進入拖曳的那一刻才：setPointerCapture ＋ 鎖捲動 ＋ 震一下 ＋ 把手變珊瑚
```

四條配套，缺一條就會半殘：

1. **`touch-action` 從 `none` 改成 `pan-y`** —— 平常放行，垂直捲動一路交給瀏覽器。
2. **鎖捲動要用 `touchmove` 的 `preventDefault()`，不能靠改 `touch-action`。**
   `touch-action` 在**手勢一開始**就決定了，中途改沒有用。
   所以：`document.addEventListener("touchmove", e => { if(pkDrag) e.preventDefault(); }, {passive:false})`。
   真機上「按住不動 220ms → 開始垂直拖」時 pan 還沒開始，這個 preventDefault 攔得住。
3. **快速滑過時瀏覽器會發 `pointercancel`** —— 待命狀態要監聽它並放棄（免費的第二道保險）。
4. **「充能」要看得見**：按住的那 220ms，把手底下的 30×30 圓角方塊底色從透明長成 `#efe8db`
   （CSS `transition:background .2s linear`，時間軸剛好對齊門檻）。
   **「還沒開始拖」是看得見的，誤按可以立刻鬆手。** 真的進入拖曳時再變 `--acc` ＋ `navigator.vibrate(12)`。

### dev 必須照做的一條（施工時實測抓到）

待命期間**不可以**把原點跟著手指移（「方向還不明就重設基準」那種寫法）：

```js
/* 錯：原點一直往前挪，橫向門檻永遠累積不到 —— 實測「往左帶 24px」完全不會觸發 */
if(Math.abs(dx)>6){ pend.x = e.clientX; pend.y = e.clientY; }
```

原點必須固定；小幅晃動交給 220ms 倒數處理就好。

### 把手的視覺重量也要降（同時治「醜」）

- **命中區維持 44×56，一格都不准縮**（不能為了不誤觸就把按鈕縮小）。
- 圖形換成 **6 個點的 inline SVG（12×18，`fill:currentColor`，`#cfc5b3`）**，
  取代 19px 的 `☰` 字符 —— 同樣是 v2.7 對 `.map-btn` 的處理：
  **把視覺重量拿掉、命中區留著**。它不再是一顆按鈕，是一塊可以抓的紋理。

### 實測結果（同一組手勢，兩種模式各跑一次）

| 手勢 | 現況（一碰就拖） | v2（門檻式） |
|---|---|---|
| 拇指沿右緣**快速**上滑 40px | **開始拖曳**（誤觸） | 沒有拖曳 ✅ |
| **慢速**垂直滑 40px（10 步，每步 4px／30ms） | — | 沒有拖曳 ✅ |
| 按住 300ms 再往上拖 | — | 拖得動 ✅ |
| 把手往左帶 24px | — | 拖得動 ✅ |

## E3. 版面修正（治「醜」）

**一區 ＝ 一張白卡，裡面用 hairline 分列**（＝沿用 App 自己的 `.mem-card` + `.mem-row` 語言）。

- `.pk-list` 變成那張卡：`background:var(--card); border-radius:18px; box-shadow:var(--shadow)`。
- 每一列 `.pk-card` 拿掉背景／圓角／陰影／margin，改成 `border-top:1px solid #f4eee2`。
- **包重新變成畫面上唯一的第二層容器**：白抽屜裡的一個**淡底盒子**
  （`#fbf7f0` ＋ `1px solid #f1eadd` ＋ `border-radius:14px` ＋ `margin:8px`）。層級一眼就回來了。
- 就地新增變成**抽屜的最後一列**（`border-top` hairline，不再是一個虛線方框）—— 少一個矩形。
  **包裡面的 `.pk-inner .pk-add` 維持虛線**（那是盒子內的次要入口，不要一起改掉）。
- 區標題退回 App 標準的 `.sec-title` 語言：`13.5px / 800 / var(--muted)`，「（上飛機帶著）」再淡一階。
- 計數改成小藥丸（`#f3eee4` / `#8f8578`，開啟篩選才變 `#fff1ef` / `--acc-deep`），
  **藥丸只有 26px 高，命中區靠外層 44px 撐**（`.pk-cnt` ≥44、內層 `.cp` 只管視覺）。
- 頂部整列重做：`[還要帶 13 樣 ▬▬▬░░][＋ 新增][📦 模板]`，
  **珊瑚只留給進度**，兩顆工具降成白底 `.pk-lite`（同 `.pk-tpl` / `.edit-toggle` 語言）。
  整列 46px，比現況矮 4px。進度條**沿用 `.pk-sub .bar` 的 4px 純珊瑚語言，不要用花費頁的漸層 `.prog`**
  （一個 App 裡「進度」只准有一種長相）。

**實測前後對照**

| | v2.9 現況 | v2 新版 |
|---|---|---|
| 有陰影的元素 | 21 | **5** |
| 頁面高（包收合） | 1930px | **1794px**（−136） |
| 頁面高（包全開） | 2962px | **2834px**（−128） |
| 最大飽和色塊 | `.pk-new` 12,820px² | 打勾圈 **729px²**（1/17.6） |
| 區標題 | 15px/800/#2b2620 | 13.5px/800/#8f8578 |
| 文字可用寬 | 250px | 248px（沒有變窄） |
| 觸控目標全掃（包全開） | 115 個，**0 個 <44px** | 115 個，**0 個 <44px** |
| input `font-size` | 16px | 16px |
| 拿起來的瞬間下面那列位移 | 0px | **0px** |

## E4. dev 要改哪裡（`public/`；這一輪不動資料格式）

1. **`public/styles.css`**（正式版不需要 A/B，直接改成新版）
   - `.pk-list`：加 `background:var(--card); border-radius:18px; box-shadow:var(--shadow); padding:2px 0 0;`
   - `.pk-card`：拿掉 `background` / `box-shadow` / `border-radius` / `margin-bottom`，
     改 `border-top:1px solid #f4eee2`；加 `.pk-list > .pk-card:first-of-type{border-top:none}`。
   - `.pk-bag{background:#fbf7f0; border:1px solid #f1eadd; border-radius:14px; margin:8px;}`
     ＋ `.pk-bag .pk-inner{border-top:1px solid #efe7d8}`、`.pk-inner::before{background:#e7dfcd}`、
     `.pk-sub-row{border-bottom-color:#f2ebdd}`、`.pk-bag + .pk-card{border-top:none}`。
     ⚠️ 一樣的老坑：**包頭專用的規則一律綁 `.pk-head-row`**，不要寫 `.pk-bag .t`。
   - `.pk-list > .pk-add`：`border:none; border-top:1px solid #f4eee2; border-radius:0; margin:0; min-height:54px; padding-left:16px;`
   - `.pack-head .t{font-size:13.5px; color:var(--muted)}` ＋ 新增 `.pack-head .t .sub{color:#b3a996; font-weight:600}`。
   - `.pk-cnt`：外層只管 44px 命中區，視覺移到內層 `.cp`（`viewPack` 的 markup 要跟著加一層 span）。
   - 刪掉 `.pk-top .pk-new`；新增 `.pk-prog`（白藥丸 ＋ 4px 珊瑚條）與 `.pk-lite`。
   - `.pk-grip`：`touch-action` **從 `none` 改成 `pan-y`**；拿掉 `font-size:19px`；
     新增 `.pk-grip .gv`（30×30、`border-radius:10px`、`transition:background .2s linear`）、
     `.pk-grip .gd`（12×18 SVG、`fill:currentColor`）、
     `.pk-grip.arming .gv{background:#efe8db; color:#8f8578; transform:scale(1.06)}`、
     `.pk-grip.hot .gv{background:var(--acc); color:#fff}`。
   - 新增 `html.drag-lock, html.drag-lock body{touch-action:none; overscroll-behavior:none;}`。
2. **`public/app.js`**
   - `pkTopBar()`：改成「進度藥丸 ＋ 兩顆輕量鈕」。
     分母 ＝ `t.packing.filter(p => !p.bag)`（**包算 1 件**，跟區的計數同一套口徑）。
   - `pkGripHtml()`：`onpointerdown` 改叫 **`packGripDown`**；內容換成 6 點 SVG；
     `onpointermove` / `up` / `cancel` 從 inline 拿掉 —— **待命期與拖曳期都改掛在 `window` 上**。
   - 新增 `packGripDown` / `pkPendMove` / `pkClearPend` / `pkArmDrag`；
     `packDragStart` 拆成 `pkBeginDrag(id, x, y, gripEl, pointerId)`。
     常數 `V_ESC=8` / `H_ARM=12` / `T_HOLD=220` **寫成檔頭的具名常數**（之後要調就改一個值）。
   - 全域一次：`document.addEventListener("touchmove", e => { if(pkDrag) e.preventDefault(); }, {passive:false})`。
   - `pkCleanupDrag`：要 `removeEventListener`、拿掉 `<html>` 的 `drag-lock`、清掉把手的 `hot` / `arming`。
   - `pkSectionHtml`：區標題加 `<span class="sub">`、計數包一層 `<span class="cp">`。
   - ⚠️ **不要動 `dragStart` / `dragMove` / `dragEnd`（行程那一套）**，也不要把門檻套過去 ——
     行程的把手只在「調整模式」出現，本來就不會誤觸，改了只會讓那邊變難拖。
3. **`APP_VER` ＋ `sw.js` 的兩個 cache 版本號一起 +1**（動手前先看檔案現況與 `git log`，別撞號）。
4. **不用改 `server.js`**：這一輪沒有動資料格式（`kind` / `bag` 不變）。

## E5. QA 檢查點（都要附實測值，不准用字級／推算）

- 量測前先鎖 device metrics **375×812**，並斷言 `innerWidth===375`（headless 預設不是 375）。
- **誤觸要有負控組**：先把把手改回「一碰就拖」跑同一組手勢，**必須測得出誤觸**；
  測不出來代表尺壞了，這一輪的綠燈全部不算。
- 四個手勢逐一驗：快速上滑 40px（不可拖）／慢速垂直 40px（不可拖）／
  按住 300ms 再拖（要拖得動）／往左帶 24px（要拖得動）。
- `.pk-grip` 的 `getComputedStyle().touchAction` 必須是 `pan-y`；拖曳中 `<html>` 必須有 `drag-lock`。
- 觸控目標**全掃**（`querySelectorAll('#app button,#app input,#app a')`）逐一驗 ≥44px，**不准列白名單**；
  掃到少於 20 個就判定尺壞了。（v2 demo 實測：包全開時掃 115 個、0 個不合格。）
- 所有 input `font-size` ＝ 16px。（實測：16px。）
- **各勾各的**：勾包之後把包**重新打開**，逐一比對「每一列的 `done` ↔ `text-decoration-line`」，
  mismatch 必須是 0，且包內 `done` 陣列前後完全相同。
  ⚠️ 勾了包會自動收合，**沒有重新打開就量 ＝ 量到 0 列，會拿到假的綠燈**（這一輪踩過一次）。
- 「已打包 x / y」分母 ＝ 該區頂層項目（包算 1）。
  （實測：`已打包 4 / 11`、`已打包 2 / 8`，`topOf()` 長度 11 / 8。）
- 拿起來的瞬間，下面某一列的 `getBoundingClientRect().top` 位移必須是 **0**。
- 既有功能不能壞：行程分頁的拖曳排序（另一套，不准被門檻污染）、v2.8 銜接條、鑰匙圈解鎖。

## E6. 要 Benson 拍板的（demo 的「⚙ DEMO 設定」可即時切，最上面兩顆大鈕一鍵對照）

1. **清單長什麼樣** —— **A 一區一張卡＋細線分列（推薦）** vs B 每一筆各自一張浮卡（現況）。
   推薦 A：層級回來了（包重新變成唯一的容器）、陰影 21→5、頁面短 136px，
   而且那是 App 自己處理長清單的既有語言。
2. **頂部那一列** —— **A 進度＋輕量鈕、珊瑚只給進度（推薦）** vs B 珊瑚實心主按鈕（現況）。
3. **把手怎麼觸發** —— **A 按住 0.22 秒／往旁邊帶一下才拖（推薦）** vs B 一碰就拖（現況）
   vs C 要先按「調整」才有把手。
   不建議 C：移動是高頻零散動作，多兩個步驟；而且 A 已經把誤觸壓到 0。
   （C 在 demo 裡仍然吃 A 的門檻 —— 真要選 C，門檻留著只有好處。）
4. （順帶，dev 層可自行調，不必等拍板）**把手的圖形**：現在是 6 個點的淡色紋理，取代 `☰`。
   若覺得太淡可以再加深一階（`#cfc5b3` → `#c2b6a1`）。
